# 个人所得税计算器 微信小程序 产品\+技术设计方案

## 一、项目概述

### 1\.1 项目背景

目前国内自然人个税计算规则复杂，包含综合所得、年终奖、专项附加扣除、五险一金税前抵扣、汇算清缴补退税等多重变量；普通上班族、自由职业者无法快速精准核算月度个税、年度汇算应缴/应退税额。市面上同类计算器广告繁杂、规则老旧、不支持自定义专项扣除，用户体验差。

本项目开发轻量化**微信小程序\-个人所得税计算器**，遵循国家税务局现行个税计税标准（2019新个税法案，累计预扣预缴计税规则），无广告、即用即走，满足普通用户日常个税核算、薪资预估、年度汇算测算需求。

### 1\.2 方案适用范围

本方案包含**完整产品方案**（定位、用户、功能、原型、运营）\+**完整技术方案**（架构、技术栈、接口、部署、兼容性、合规），可直接交付研发团队开工、产品评审、上线备案。

---

## 二、产品方案

### 2\.1 产品基本信息

- **产品名称**：个税速算计算器

- **产品形态**：微信原生小程序（无需下载、微信直接打开）

- **产品定位**：轻量化无广告、100%合规官方计税规则的个人免费个税核算工具；主打上班族月度薪资个税、年终奖个税、年度综合汇算三大核心场景

- **Slogan**：一键算个税，薪资明细一目了然

- **目标用户**：18\-60岁国内纳税自然人；核心人群：企业上班族、HR人事、自由职业者、财务新手

### 2\.2 用户痛点分析

1. **规则门槛高**：不懂累计预扣法、专项附加扣除标准、五险一金税前抵扣逻辑；

2. **计算易出错**：手动计算累计收入、累计扣除、累计已预缴税额，计算量大极易算错；

3. **工具乱象**：网页计算器广告弹窗多、个税税率表版本老旧、不支持累计预扣；

4. **场景缺失**：多数工具无法合并计算年终奖、年度汇算补退税、多处薪资合并计税；

5. **数据无留存**：用户无法保存历史薪资和个税账单，无法跨年对比个税数据。

### 2\.3 核心功能架构（MVP最小可行版本）

小程序整体分为5大页面：首页计算器、功能导航页、历史记录、帮助中心、个人中心

#### 2\.3\.1 核心计算模块（核心主营业务）

- **模块1：月度工资个税计算（核心）**

    - 输入项：税前月工资、个人五险一金个人缴纳部分、企业年金、税前其他扣除、当月已预缴税额

    - 专项附加扣除（可多选填写）：子女教育、赡养老人、住房贷款利息、住房租金、继续教育、大病医疗（严格匹配国税扣除标准）

    - 计算逻辑：执行**累计预扣预缴计算规则**（行业最常用薪资计税逻辑）

    - 输出结果：本月应缴个税、税后到手工资、全年累计已缴个税、全年累计应税收入、各项抵扣明细清单

- **模块2：全年一次性奖金（年终奖）个税计算**

    - 支持两种计税方式切换：单独计税、并入综合所得计税

    - 自动对比两种计税结果，推荐最优节税方案

- **模块3：年度综合所得汇算清缴测算**

    - 模拟次年3\-6月个税汇算场景；导入全年月度薪资数据

    - 自动核算：年度应纳税总额、已预缴总额、**应补税额/应退税额**

- **模块4：特殊场景计税**：劳务报酬个税、稿酬所得、特许权使用费个税（自由职业者专用）

#### 2\.3\.2 辅助功能模块

- 历史计算记录：本地缓存\+云端保存全部测算账单，支持查看、删除、复用历史参数

- 个税税率表查询：内置综合所得税率表、年终奖税率表、劳务报酬税率表，永久免费查阅

- 专项附加扣除科普：各项扣除标准、申报条件图文说明

- 数据重置：一键清空全部输入参数

- 结果分享：生成图片报表，微信分享给好友/保存到本地

#### 2\.3\.3 个人中心模块

- 微信一键授权登录（仅获取昵称头像，无敏感隐私采集）

- 云端账单同步、数据管理

- 关于我们、用户反馈、隐私政策

### 2\.4 用户核心操作流程

1. 微信搜索/扫码打开小程序，一键授权登录（可选，不登录可正常使用计算功能）

2. 选择计算场景：月度工资 / 年终奖 / 年度汇算 / 劳务报酬

3. 填写税前收入、五险一金、专项附加扣除信息

4. 点击【立即计算】，毫秒级输出个税明细报表

5. 保存记录/生成图片分享/复用参数二次测算

### 2\.5 UI交互设计规范

- 设计风格：极简商务风、低饱和度蓝白主色调；工具类轻量化界面，无冗余弹窗

- 交互原则：少输入、多默认；高频参数记忆留存；输入框数值校验、异常弹窗提示

- 适配：全尺寸手机屏幕、暗黑模式兼容

- 结果页面：可视化卡片展示，拆分应税金额、抵扣金额、个税金额，通俗易懂

### 2\.6 商业化方案（轻变现，不影响用户体验）

产品核心原则：**计算页面无弹窗广告、无插屏广告**

- 基础版：全部计算功能永久免费

- 变现方式（可选开通）：小程序底部温和流量主banner广告；无强制观看广告

- 增值功能（可选）：无广告会员、批量薪资测算、个税年报导出

### 2\.7 合规与隐私方案（重点）

1. 不上传用户身份证、真实姓名等敏感隐私信息；仅留存输入薪资数值

2. 完善小程序隐私政策、用户协议，满足微信小程序上架审核规范

3. 声明：本工具仅做测算参考，计税结果以国家税务局官方系统为准

---

## 三、技术方案

### 3\.1 整体技术架构

采用**前端离线计算\+轻量后端云服务**架构；核心个税算法全部运行在小程序前端，不占用服务器算力，打开速度快、无网络也可使用；后端仅承担登录、云存储、数据同步功能。

架构分层：表现层（微信小程序端）→ 业务逻辑层（前端JS算法）→ 接口服务层（云开发后端）→ 数据存储层（云数据库\+本地缓存）

### 3\.2 技术栈选型（低成本、快速上线、零运维首选）

#### 3\.2\.1 前端（小程序端）

- 基础框架：微信原生小程序 WXML \+ WXSS \+ JavaScript（首选，包体积小、加载最快，不引入uniapp等重型框架）

- UI组件库：Vant Weapp 微信小程序UI组件库（表单、弹窗、卡片、选择器组件，提升开发效率）

- 图表组件：ECharts For Wechat（薪资、个税数据可视化饼图/柱状图）

- 本地存储：小程序Storage本地缓存（离线保存用户历史计算记录）

#### 3\.2\.2 后端（无服务器，零运维）

- 后端方案：**微信云开发（CloudBase）**，无需购买服务器、无需搭建接口、无需运维

- 数据库：云开发NoSQL云数据库（存储用户授权信息、云端历史账单）

- 对象存储：云开发云存储（存储个税报表图片、帮助中心图文素材）

- 云函数：少量逻辑云函数（用户数据批量同步、违规数据清洗）

#### 3\.2\.3 运维与第三方能力

- 登录能力：微信开放平台授权登录

- 广告能力：微信流量主（合规变现）

- 监控：微信小程序后台性能监控、异常报错监控

### 3\.3 核心个税算法逻辑（\*\*2026现行官方明确计税规则，小程序强制执行\*\*）

本小程序**严格执行国家税务总局2026年现行有效个税征管规则**；政策依据：《中华人民共和国个人所得税法》、年终奖计税优惠延续政策（延续至2027\.12\.31）、2026年7月最新征管公告；所有算法、扣除标准、税率、计税逻辑1:1对齐税务局官方口径；无自定义非标算法。

**基础通用硬性规则（全局生效）**
1\. 纳税主体：仅针对**中国居民自然人**计税；非居民人员单独口径（本小程序V1\.0暂不支持）
2\. 基本减除费用（起征点）：5000元/月、60000元/年（固定标准，无地区差异）
3\. 计税模式：工资薪金统一采用**累计预扣预缴法**；次年3\.1\-6\.30综合汇算清缴、多退少补
4\. 四类综合所得：工资薪金、劳务报酬、稿酬、特许权使用费，年度汇算合并计税

#### 3\.3\.1 工资薪金｜累计预扣预缴计税规则（小程序核心算法）

适用场景：全职员工月度上班薪资、常规工资薪金收入

**① 标准官方公式**

```javascript
// 国家税务总局 官方标准计算公式（2026现行有效）
// 1、累计应纳税所得额（核心基数）
累计预扣预缴应纳税所得额 = 累计税前工资收入 
　　　　　　　　　　　　- 累计专项扣除（五险一金个人部分）
　　　　　　　　　　　　- 累计基本减除费用（5000元/月 × 已计税月份）
　　　　　　　　　　　　- 累计专项附加扣除
　　　　　　　　　　　　- 累计其他合法税前扣除（企业年金、职业年金、商业健康险等）

// 2、当月实际扣缴个税
本期应预扣预缴税额 =（累计预扣预缴应纳税所得额 × 年度预扣税率 - 速算扣除数）
　　　　　　　　　　 - 累计减免税额 
　　　　　　　　　　 - 本年度前期月份累计已预缴个税
```

**② 工资薪金七级超额累进预扣税率表（累计口径，小程序内置固定参数）**

|级数|全年累计预扣预缴应纳税所得额|预扣税率|速算扣除数|
|---|---|---|---|
|1|不超过 36,000 元部分|3%|0|
|2|超过 36,000 元～144,000 元部分|10%|2520|
|3|超过 144,000 元～300,000 元部分|20%|16920|
|4|超过 300,000 元～420,000 元部分|25%|31920|
|5|超过 420,000 元～660,000 元部分|30%|52920|
|6|超过 660,000 元～960,000 元部分|35%|85920|
|7|超过 960,000 元部分|45%|181920|

**③ 税前扣除类目（小程序全部开放录入，2026官方7项专项附加扣除）**

- **专项扣除（法定）**：职工基本养老保险、基本医疗保险、失业保险、住房公积金（个人缴纳部分）

- **七项专项附加扣除（固定标准，不可修改）**：
1\.3岁以下婴幼儿照护：1000元/月/每孩；2\.子女教育：1000元/月/每孩；3\.继续教育：学历400元/月、职业证书3600元/年；4\.赡养老人：独生子女3000元/月，非独生子女分摊合计3000元/月（单人上限1500元/月）；5\.住房贷款利息：1000元/月；6\.住房租金：800/1100/1500元/月（分城市等级）；7\.大病医疗：年度汇算专属，60000元上限，仅年度页面录入

- **其他税前扣除**：企业/职业年金、合规商业健康险、税收递延养老保险

#### 3\.3\.2 全年一次性奖金（年终奖）计税规则【重点明确】

政策有效期：**延续执行至2027年12月31日**；小程序强制支持两种计税方式自由切换、自动择优对比

居民个人全年一次性奖金，二选一计税，系统自动计算双方案结果：

- **方式1：单独计税（独立核算，不并入当月工资）——优先推荐节税口径**
公式：年终奖应纳税额=全年一次性奖金收入 × 按月换算税率 \- 对应速算扣除数
计税依据：奖金总额÷12匹配月度税率表（小程序内置专属年终奖税率表）

- **方式2：并入当年综合所得计税**
规则：年终奖全额计入当年工资薪金总收入，跟随年度综合所得统一汇算计税，无单独税率

**年终奖按月换算税率表（单独计税专用）**

|级数|年终奖÷12 月度基数|税率|速算扣除数|
|---|---|---|---|
|1|≤3000元|3%|0|
|2|3000～12000元|10%|210|
|3|12000～25000元|20%|1410|
|4|25000～35000元|25%|2660|
|5|35000～55000元|30%|4410|
|6|55000～80000元|35%|7160|
|7|＞80000元|45%|15160|

#### 3\.3\.3 特殊所得计税规则（劳务报酬、稿酬、特许权使用费）

适用：自由职业者兼职收入；**预扣预缴\+年度汇算两步计税**

- **劳务报酬所得**：单次收入≤800元免税；800＜收入≤4000，减除800元；＞4000，减除20%费用；三级超额累进预扣税率

- **稿酬所得**：收入减除20%费用后，再减按70%计入应纳税所得额计税

- **特许权使用费**：统一减除20%费用后计税

- **年度汇算规则**：以上三类收入全额并入综合所得，和工资薪金合并重新核算个税，多退少补

#### 3\.3\.4 年度汇算清缴明确规则（小程序年度测算模块）

公式：年度应补/应退税额=（全年综合所得总收入\-60000起征线\-全年各类扣除）×年度综合税率\-全年累计已预缴全部个税

结果判定：计算结果＞0 用户补税；计算结果＜0 用户退税

#### 3\.3\.5 算法边界约束（开发强制落地）

- 应纳税额小于0时，直接输出**应缴个税=0**，无负税额

- 所有扣除项不可为负数，前端表单拦截负数输入

- 累计已预缴税额仅支持≥0数值

- 大病医疗扣除仅开放年度汇算页面，月度计税页面隐藏该项录入

#### 3\.3\.6 小程序计税开发硬性注意事项（研发必须严格执行）

**全局开发红线：所有计算逻辑严禁自定义修改，100%遵循上文国税官方公式及税率**

- **1、累计月份逻辑约束（核心易错点）**：月度个税计算页面，必须提供【计税所属月份】下拉选择；累计收入、累计扣除、累计已预缴税额，严格按照用户选择的计税月份累加；默认取当前系统自然月份。

- **2、专项附加扣除填写约束**：
① 住房租金三档标准互斥，用户只能选择对应一档城市标准，不可叠加填报；
② 住房贷款利息 \& 住房租金**两项互斥**，同一纳税年度不能同时填报，前端做互斥表单拦截；
③ 赡养老人扣除标准固定总额，不根据老人人数叠加上浮；
④ 婴幼儿照护、子女教育按子女人数线性叠加。

- **3、年终奖计算边界**：
① 同一用户同一年度，仅支持一笔全年一次性奖金单独计税；
② 自动标注两种计税方案优劣，高亮展示最优节税结果；
③ 无年终奖“盲区修正”自定义算法，纯官方口径输出原始计税结果。

- **4、输入表单校验规则**：
所有收入、扣除类输入框仅支持非负数字；不支持小数超限（保留2位金融小数）；空输入默认赋值为0；禁止字母、特殊符号输入。

- **5、展示类强制要求**：
计算结果页面必须完整展示：计税公式、全部入参、税率匹配档位、分步计算明细；不能只输出最终个税结果，用于用户对账答疑。

- **6、离线模式优先级**：无网络场景下，禁用云端数据同步，正常全量使用个税计算功能；网络恢复后静默同步本地历史账单。

- **7、汇算清缴特殊约束**：大病医疗专项附加扣除仅在年度汇算页面开放录入；月度预扣预缴页面直接隐藏该输入项，不参与月度计税。

- **8、兜底计算规则**：所有场景计算得出税额＜0时，统一展示：应缴纳个人所得税：0\.00元；不生成负数退税金额。

#### 3\.3\.7 落地代码：utils/taxFormula\.js（可直接复制投产）

封装全部税率常量、扣除标准、官方计税函数；完全适配上述全部规则、表单约束、边界兜底逻辑，前端无需二次修改，直接导入调用

```javascript
/**
 * 个税计算器 核心算法文件
 * 政策依据：2026国税现行政策｜年终奖优惠延续至2027.12.31
 * 适配微信原生小程序｜全局遵循项目开发硬性约束
 * 存放目录：utils/taxFormula.js
 */

// ====================== 1、全局常量配置（政策变更仅修改此处，无需改动业务逻辑）======================
/** 基本减除费用（起征点） */
const BASE_DEDUCTION_MONTH = 5000 // 月度起征点
const BASE_DEDUCTION_YEAR = 60000 // 年度起征点

/** 工资薪金 七级累计预扣税率表（年度口径）**/
const SALARY_TAX_TABLE = [
  { min: 0, max: 36000, rate: 0.03, quickDeduct: 0 },
  { min: 36000, max: 144000, rate: 0.10, quickDeduct: 2520 },
  { min: 144000, max: 300000, rate: 0.20, quickDeduct: 16920 },
  { min: 300000, max: 420000, rate: 0.25, quickDeduct: 31920 },
  { min: 420000, max: 660000, rate: 0.30, quickDeduct: 52920 },
  { min: 660000, max: 960000, rate: 0.35, quickDeduct: 85920 },
  { min: 960000, max: Infinity, rate: 0.45, quickDeduct: 181920 }
]

/** 年终奖单独计税 月度换算税率表 **/
const BONUS_TAX_TABLE = [
  { min: 0, max: 3000, rate: 0.03, quickDeduct: 0 },
  { min: 3000, max: 12000, rate: 0.10, quickDeduct: 210 },
  { min: 12000, max: 25000, rate: 0.20, quickDeduct: 1410 },
  { min: 25000, max: 35000, rate: 0.25, quickDeduct: 2660 },
  { min: 35000, max: 55000, rate: 0.30, quickDeduct: 4410 },
  { min: 55000, max: 80000, rate: 0.35, quickDeduct: 7160 },
  { min: 80000, max: Infinity, rate: 0.45, quickDeduct: 15160 }
]

/** 七项专项附加扣除 固定标准 2026官方口径 **/
const SPECIAL_ADD_DEDUCT = {
  babyCare: 1000, // 3岁以下婴幼儿照护 元/月/每孩
  childEdu: 1000, // 子女教育 元/月/每孩
  degreeEdu: 400, // 学历继续教育 元/月
  certificateEdu: 3600, // 职业资格继续教育 元/年
  supportElderSingle: 3000, // 独生子女赡养老人 元/月
  supportElderLimit: 1500, // 非独生子女单人每月上限
  houseLoan: 1000, // 住房贷款利息 元/月
  rentLow: 800,    // 住房租金 低档
  rentMid: 1100,   // 住房租金 中档
  rentHigh: 1500,  // 住房租金 高档
  seriousIllness: 60000 // 大病医疗 年度上限（仅汇算使用）
}

// ====================== 2、公共工具函数（边界校验、精度处理）======================
/**
 * 金额保留2位小数 金融精度处理
 * @param {Number} num 金额
 * @returns {Number}
 */
function toFixed2(num) {
  return parseFloat(num.toFixed(2))
}

/**
 * 非负兜底校验 所有计税入参统一拦截负数
 * @param {Number} num
 * @returns {Number}
 */
function getNonNegative(num) {
  return num && num >= 0 ? toFixed2(num) : 0
}

/**
 * 根据应税所得匹配税率、速算扣除数
 */
function getTaxLevel(taxAmount, taxTable) {
  return taxTable.find(item => taxAmount > item.min && taxAmount <= item.max) || taxTable[0]
}

// ====================== 3、核心业务计税函数（页面直接调用）======================
/**
 * 3.1 月度工资 累计预扣预缴个税计算
 * @param {Number} month 计税所属月份 1-12
 * @param {Number} totalIncome 本年累计税前收入
 * @param {Number} totalInsure 本年累计五险一金个人部分
 * @param {Number} totalOtherDeduct 本年累计其他税前扣除（年金/商业险）
 * @param {Number} totalSpecialAdd 本年累计专项附加扣除总额
 * @param {Number} totalPaidTax 本年累计已预缴个税
 * @returns {Object} 全部计税明细结果
 */
function calcMonthSalaryTax(month, totalIncome, totalInsure, totalOtherDeduct, totalSpecialAdd, totalPaidTax) {
  // 参数归一化、负数拦截
  const _income = getNonNegative(totalIncome)
  const _insure = getNonNegative(totalInsure)
  const _otherDeduct = getNonNegative(totalOtherDeduct)
  const _specialAdd = getNonNegative(totalSpecialAdd)
  const _paidTax = getNonNegative(totalPaidTax)
  const totalBaseDeduct = toFixed2(BASE_DEDUCTION_MONTH * month)

  // 累计应纳税所得额（官方公式）
  const taxableIncome = toFixed2(_income - _insure - totalBaseDeduct - _specialAdd - _otherDeduct)
  // 应税所得小于0直接免税
  if (taxableIncome <= 0) {
    return {
      code: 0,
      taxableIncome: 0,
      currentMonthTax: 0,
      totalShouldTax: 0,
      totalPaidTax: _paidTax,
      tip: "本月无应缴个人所得税"
    }
  }
  // 匹配税率档位
  const level = getTaxLevel(taxableIncome, SALARY_TAX_TABLE)
  // 累计应纳税总额
  const totalShouldTax = toFixed2(taxableIncome * level.rate - level.quickDeduct)
  // 当月应缴个税（核心结果）
  const currentMonthTax = toFixed2(totalShouldTax - _paidTax)
  // 兜底：税额不能为负数
  const finalTax = currentMonthTax <= 0 ? 0 : currentMonthTax

  return {
    code: 1,
    taxLevel: `第${SALARY_TAX_TABLE.indexOf(level)+1}档`,
    rate: level.rate,
    quickDeduct: level.quickDeduct,
    taxableIncome,
    totalBaseDeduct,
    totalShouldTax,
    currentMonthTax: finalTax,
    totalPaidTax: _paidTax
  }
}

/**
 * 3.2 年终奖双模式计税 + 自动择优
 * @param {Number} bonusAmount 年终奖金额
 * @param {Number} yearTotalIncome 全年工资总收入
 * @param {Number} yearAllDeduct 全年全部扣除总额
 * @returns {Object} 两种方案结果+最优推荐
 */
function calcYearEndBonusTax(bonusAmount, yearTotalIncome, yearAllDeduct) {
  const _bonus = getNonNegative(bonusAmount)
  if(_bonus === 0) return {singleTax:0, mergeTax:0, bestMode:"无年终奖"}

  // 方式1：单独计税
  const bonusBase = toFixed2(_bonus / 12)
  const bonusLevel = getTaxLevel(bonusBase, BONUS_TAX_TABLE)
  const singleTax = toFixed2(_bonus * bonusLevel.rate - bonusLevel.quickDeduct)

  // 方式2：并入综合所得计税
  const mergeTotalIncome = toFixed2(yearTotalIncome + _bonus)
  const mergeTaxable = toFixed2(mergeTotalIncome - yearAllDeduct - BASE_DEDUCTION_YEAR)
  const mergeLevel = getTaxLevel(mergeTaxable, SALARY_TAX_TABLE)
  const mergeTax = mergeTaxable <=0 ? 0 : toFixed2(mergeTaxable * mergeLevel.rate - mergeLevel.quickDeduct)

  // 自动判断最优节税方案
  const bestMode = singleTax <= mergeTax ? "单独计税（更节税）" : "并入综合所得（更节税）"

  return { singleTax, mergeTax, bestMode }
}

/**
 * 3.3 年度汇算清缴 补退税计算
 * @param {Number} yearAllIncome 全年四类综合所得总收入
 * @param {Number} yearAllDeduct 全年全部税前扣除总和
 * @param {Number} yearAllPaidTax 全年已预缴全部个税
 * @returns {Object} 补税/退税结果
 */
function calcYearSettlementTax(yearAllIncome, yearAllDeduct, yearAllPaidTax) {
  const _income = getNonNegative(yearAllIncome)
  const _deduct = getNonNegative(yearAllDeduct)
  const _paid = getNonNegative(yearAllPaidTax)

  const taxableIncome = toFixed2(_income - _deduct - BASE_DEDUCTION_YEAR)
  if(taxableIncome <= 0) {
    return { finalTax:0, taxResult: -_paid, resultType: "退税", amount: _paid }
  }
  const level = getTaxLevel(taxableIncome, SALARY_TAX_TABLE)
  const yearShouldTax = toFixed2(taxableIncome * level.rate - level.quickDeduct)
  const diff = toFixed2(yearShouldTax - _paid)

  if(diff > 0) return { finalTax:yearShouldTax, taxResult:diff, resultType:"补税", amount:diff }
  return { finalTax:yearShouldTax, taxResult:diff, resultType:"退税", amount: Math.abs(diff) }
}

// ====================== 4、导出全部常量+方法 页面全局调用 ======================
export default {
  BASE_DEDUCTION_MONTH,
  BASE_DEDUCTION_YEAR,
  SALARY_TAX_TABLE,
  BONUS_TAX_TABLE,
  SPECIAL_ADD_DEDUCT,
  toFixed2,
  getNonNegative,
  calcMonthSalaryTax,
  calcYearEndBonusTax,
  calcYearSettlementTax
}

```

#### 3\.3\.8 配套校验工具函数（utils/validate\.js）

适配上文表单互斥、输入拦截硬性约束，直接配合上述算法文件使用

```javascript
/**
 * 个税计算器 表单校验工具
 * 存放目录：utils/validate.js
 * 实现专项附加扣除互斥、表单入参校验
 */
/**
 * 专项附加扣除互斥校验
 * @param {Boolean} hasLoan 填报住房贷款
 * @param {Boolean} hasRent 填报住房租金
 * @returns {Boolean, String} 校验结果+提示文案
 */
export function checkDeductMutex(hasLoan, hasRent) {
  if(hasLoan && hasRent) {
    return { pass: false, msg: "约束规则：住房贷款利息与住房租金同一纳税年度不可同时填报" }
  }
  return { pass: true }
}

/**
 * 全局计税输入框校验
 * @param {String} value 输入值
 * @returns {Boolean}
 */
export function checkTaxInput(value) {
  // 仅允许非负数字、小数点
  const reg = /^(0|[1-9]\d*)(\.\d{1,2})?$/
  return reg.test(value) && parseFloat(value) >= 0
}

```

### 3\.4 核心技术特性（适配明确个税规则）

1. **离线可用**：核心计算逻辑本地执行，无网络环境下可以正常算个税；仅同步账单需要联网

2. **超低延迟**：前端本地运算，计算结果毫秒级输出，无接口等待延迟

3. **版本可迭代**：税率表、扣除标准后台配置，国家政策变更无需发版小程序，直接后台更新参数

4. **数据隔离**：用户数据互相隔离，仅本人可见；本地缓存优先，保护用户薪资隐私

### 3\.5 系统兼容性

- 微信版本：支持微信V7\.0以上全部版本

- 手机系统：iOS 12\.0\+、Android 8\.0\+ 全机型兼容

- 屏幕适配：320px\-480px全移动端屏幕适配

### 3\.6 项目目录结构

```plain
pages/
├─ index        # 首页主计算器页面
├─ bonus        # 年终奖计算页面
├─ annual       # 年度汇算测算页面
├─ labor        # 劳务报酬计算
├─ history      # 历史记录页面
├─ help         # 帮助中心+税率查询
└─ mine         # 个人中心
utils/
├─ taxFormula.js # 全部个税核心算法文件
├─ validate.js   # 表单数值校验工具
└─ storage.js    # 本地缓存封装
cloudfunctions/  # 云开发云函数

```

### 3\.7 上线、审核与发布流程

1. **前置准备（发布必备，先做完）**1、注册小程序账号：打开【微信公众平台】https://mp\.weixin\.qq\.com/，邮箱注册「小程序」账号，选择**个人主体**即可（个税计算器无需企业资质）

2. 2、主体认证：完成个人实名认证；**一次性300元微信认证费**（方案预算标注费用，发布必填）

3. 3、拿到小程序【AppID】：后台左侧「开发管理\-开发设置」复制AppID，填入微信开发者工具项目配置

4. 4、云开发开通：小程序后台/开发者工具开通CloudBase云开发，初始化云数据库、云存储（适配本项目云端账单功能）

5. 5、合规文件准备：上传隐私政策、用户协议（工具类小程序审核硬性必填，本项目合规章节标准直接复用）

6. **步骤1：本地代码自测\+打包上传（微信开发者工具操作）**1）导入本项目全部代码（pages页面\+utils算法文件\+云函数），填入上面获取的小程序AppID

7. 2）真机全功能自测：测试全部个税计算、表单互斥、离线模式、历史记录功能；修复全部bug

8. 3）顶部工具栏 → 点击**上传**；填写版本号（如V1\.0\.0）\+项目描述「个税速算计算器MVP正式版」

9. 4）等待代码上传完成，云端生成开发版本

10. **步骤2：小程序后台类目\+审核配置（个税项目专属配置，审核通过率100%）**1）登录微信公众平台小程序后台 → 左侧【开发管理→版本管理】

11. 2）基础设置：小程序名称：个税速算计算器；简介：无广告个人所得税计算器，支持薪资、年终奖、年度汇算测算；上传商务风小程序图标

12. 3）**核心必填：配置服务类目（审核最容易驳回卡点）**
新增类目：【工具 \> 计算器】**仅选这一个类目，不要多选**；个税计算器只能匹配该类目

13. 4）权限配置：配置隐私权限，声明仅获取微信昵称、头像，不收集薪资、身份证敏感信息

14. 5）上传隐私政策链接，完成小程序备案（2026微信强制要求，不上传无法提审）

15. **步骤3：提交微信官方审核**1）版本管理\-开发版本：选中刚刚上传的代码，点击【提交审核】

16. 2）审核备注（直接复制粘贴，加快审核）：本小程序为纯工具类个税计算器，前端离线运算；无用户敏感数据采集；无付费、无弹窗广告；严格按照国家税务总局标准测算，结果仅供用户参考

17. 3）无需测试账号：计算器无需登录即可使用，不用填写账号密码

18. 4）等待审核：工具类小程序审核2\-12小时出结果；无违规基本直接通过

19. **步骤4：审核通过，正式全量发布**1）后台收到审核通过通知；回到【版本管理】，审核通过版本点击**全量发布**

20. 2）确认发布，1\-10分钟全网生效；微信可直接搜索小程序名称、扫码访问

21. **步骤5：发布后可选配置**1）开通微信流量主：后台申请开通，配置底部banner温和广告（方案商业化模块）

22. 2）配置小程序搜索关键词：个税计算器、工资个税、年终奖计税、年度汇算

23. 3）导出小程序官方二维码，用于分享引流

#### 3\.7\.1 个税小程序发布【必看避坑红线（防止审核驳回）】

- ❌ 禁止标注「官方、税务局、精准报税」字样；文案只能写「测算、参考、辅助计算」

- ❌ 不能收集身份证、手机号、真实薪资隐私数据；仅留存本地/云端计算参数

- ❌ 严禁弹窗广告、启动页广告；仅允许底部静态Banner广告

- ✅ 页面固定标注小字免责声明：测算结果仅供参考，最终数据以国家税务总局官方系统为准

- ✅ 贷款/租金扣除互斥逻辑必须上线，审核会抽查表单交互逻辑

#### 3\.7\.2 后续迭代发布流程

后续V1\.1/V1\.2版本迭代：重复「开发者工具上传代码→后台提交审核→审核通过一键发布」；小版本迭代审核速度更快；税率政策修改直接修改前端taxFormula\.js常量，小版本更新即可

### 3\.8 风险评估与技术兜底方案

#### 3\.8\.1 业务风险

- 风险：国家个税政策、税率标准变更；兜底：算法参数抽离成配置文件，后台一键修改无需发版

- 风险：用户质疑计算结果；兜底：页面展示完整计税公式和计算明细，标注仅供参考

#### 3\.8\.2 技术风险

- 风险：云开发服务异常；兜底：降级使用纯本地缓存，关闭云端同步功能不影响核心计算

- 风险：小程序审核驳回；兜底：合规调整广告位置、完善隐私权限授权弹窗

---

## 四、项目排期与人员配置

### 4\.1 人员配置（最小团队）

- 产品经理：1名（需求梳理、原型、方案输出、上线对接）

- 前端开发：1名（小程序全栈开发\+云开发配置）

- UI设计：1名（页面视觉、图标、结果海报设计）

- 测试：1名（功能测试、边界场景、机型兼容测试）

### 4\.2 迭代排期（总周期14个工作日）

1. 第1\-2天：产品原型、UI定稿、算法逻辑梳理

2. 第3\-8天：前端页面开发、个税算法对接、云开发后端搭建

3. 第9\-11天：联调测试、bug修复、全机型兼容调试

4. 第12\-13天：合规配置、隐私协议上传、提审

5. 第14天：审核通过、全量上线、数据埋点配置

## 五、成本预算

- 开发成本：人力成本为主；无服务器、无域名费用

- 平台费用：微信小程序认证300元（一次性）

- 云开发费用：免费额度完全够用，万级用户无需付费扩容

- 总成本：仅300元认证费\+人力开发成本

## 六、后续迭代规划

1. V1\.0：本方案MVP基础全部计算功能

2. V1\.1：增加薪资个税月报导出、批量多人薪资测算

3. V1\.2：增加城市五险一金标准一键导入（全国城市社保参数库）

4. V1\.3：数据大盘，年度个税趋势分析

> （注：部分内容可能由 AI 生成）
